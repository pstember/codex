# Progress

## Current Phase

Foundation and demo auth are complete. Current phase: Phase 1, Insight workbench with GraphQL validation, generic storefront adaptation generation, publishing controls, Guest rendering, rollback groundwork, Storefront Time Machine comparison, explicit Guest version selection, clean fixture packaging, and real Codex App Server custom-query support.

## Completed

- Docked admin observability like a full-page DevTools panel: the shared rail now owns independent full-height scrolling, the admin shell reserves a wide right column while open and a narrow handle column while collapsed, and the `<` reopen affordance remains available. Updated Playwright coverage for open/collapsed non-overlap, full-height dock behavior, and mobile horizontal overflow. Checks: scoped Biome check, `npm run typecheck`, `npm run lint`, `npm test`, `npm run coverage`, `npm run build`, and `npm run deps:outdated` passed. Coverage is 93.12% statements, 93.15% lines, 95.55% functions, and 81.85% branches. `npm run security:audit` still reports the known 2 moderate Next/PostCSS advisories where the force fix would downgrade Next to 9.3.3. Browser smoke was attempted against a production server on port 3007, but local Chromium launch is blocked by the macOS Mach port permission error in this environment.
- Code quality and fixture DB cleanup: removed legacy Metrics Copilot domain/persistence, stale metrics trace and campaign proposal SQLite schema, unused analytics SQLite tables, unused auth placeholder actions, the unused `approve_campaign` permission, and shopper persona cart plumbing. Added `npm run db:fixture` to generate a clean tracked `fixtures/commerce.db`, renamed the storefront draft DB column to `source_draft_key`, kept promotion analytics in TypeScript fixtures, and refreshed the ignored local `.data/commerce.db` from the clean fixture. Checks: focused cleanup tests, `npm run lint`, `npm run typecheck`, `npm test`, `npm run coverage`, `npm run build`, and `npm run deps:outdated` passed. Coverage is 93.12% statements, 93.15% lines, 95.55% functions, and 81.85% branches. `npm run security:audit` still reports the known 2 moderate Next/PostCSS advisories where the force fix would downgrade Next to 9.3.3.
- Restored raw Codex App Server exchange visibility for `/admin/insights`: the data-question stream now uses the traced App Server JSON helper, unwraps validated values for behavior, and shows the raw request envelope plus raw response text in the observability `Raw detail` blocks for both query-planning and final-answer turns. Checks: RED `tests/dataQuestion.test.ts` first failed on traced runner output, then `npm test -- tests/dataQuestion.test.ts`, `npm run typecheck`, `npm run lint`, `npm test`, and `npm run build` passed.
- Moved `/admin/storefront` version history out of the narrow compare sidebar into a full-width section below the edit/compare workbench so rollback targets read as published storefront history, not supporting metadata. Added a focused Playwright assertion that the version history is no longer inside the compare aside. Checks: `npm run typecheck` and scoped `npx biome check src/app/admin/storefront/page.tsx tests/e2e/operator-handoff.spec.ts` passed. Focused Playwright was attempted, but the Next test server still cannot bind to `127.0.0.1:3000` in this environment (`listen EPERM`).
- Fixed the storefront observability rail so save-only actions no longer appear to open it on page reload. The rail now restores its last session state from `sessionStorage`, starts closed by default, and auto-opens only for the actual Codex-backed visual generation submit. Checks: `npm run typecheck` passed.
- Clarified the storefront editor text-save control so `Save draft edits` now explicitly means manual draft persistence, while helper copy explains that only `Generate all text` creates a Codex observability trace. Checks: `npm run typecheck` passed. A direct `npm test -- tests/e2e/operator-handoff.spec.ts` attempt confirmed this repo excludes Playwright specs from Vitest, so the updated smoke assertion was not runnable through that command.
- Renamed the storefront brand-kit save action from `Save selection` to `Save brand kit` so the control matches its actual behavior of persisting palette and selection-name edits on the current draft. Checks: text search confirmed the old label only existed in the storefront studio UI.
- Added an explicit storefront `Ready to publish` review step. Generated or edited drafts now move through `invalid` or `draft` to `ready` before they can go live, publish actions are gated on `ready`, and any material edit resets approval back to `draft`. The gallery and editor now show `Mark ready to publish`, `Move back to draft`, and clearer `Draft`/`Ready`/`Current` labels. Checks: `npm test -- tests/storefrontAdaptation.test.ts tests/storefrontPublishing.test.ts tests/database.test.ts tests/storefront.test.ts tests/imageHarness.test.ts`, `npm run typecheck`, and `npm run lint` passed. Focused Playwright verification was updated but is still blocked in this environment because the local test web server cannot bind to `127.0.0.1:3000` (`listen EPERM`).
- Updated the public homepage staff version controls to use storefront gallery entries instead of published-version history, added separate `Preview` and `Apply` actions, and disabled the apply button as `Current` when the live selection is chosen. Checks: `npm test -- tests/storefrontPublishing.test.ts`, `npm run typecheck`, and focused `biome check` on the touched files passed.
- Swapped the `/admin` workspace tile order so `Storefront Studio` appears before `Insight`, and added a focused test to lock that landing-page sequence. Checks: `npm test -- tests/adminWorkspaces.test.ts` and `npm run typecheck` passed. `npm run lint` still fails because of unrelated existing Biome issues in `tests/support/fixtureCodexHarness.ts` and `src/domain/storefrontAdaptation.ts`.
- Removed shopper-facing cart promotion pricing and the seasonal campaign proposal/revamp layer. `/admin/storefront` now keeps the generic event adaptation flow as the only merchandising workflow, and storefront drafts use a neutral `sourceDraftKey` in code. Superseded note: local SQLite compatibility was later dropped and the persisted column was renamed to `source_draft_key`. Checks: `npm run typecheck` and `npm test` passed.
- Added a shopper-facing footer to `/` with About, Terms & Conditions, Privacy, and Shipping & Returns links; removed the public `Staff admin` link; and created matching mock content pages at `/about`, `/terms`, `/privacy`, and `/shipping-returns`. Checks: `npm test -- tests/storefront.test.ts`, `npm run typecheck`, `npm run lint`, and `npm run build` passed. A focused Playwright run was updated for the new footer behavior, but execution is still blocked in this environment because the local test web server cannot bind to `127.0.0.1:3000` (`listen EPERM`).
- Renamed the `/admin` workspace tile from `Manage storefront` to `Storefront Studio` and aligned the related Playwright route smoke expectations.
- Created project memory docs.
- Updated `AGENTS.md` with session start docs and non-negotiables.
- Initialized the local git repository on `main` and configured `origin` as `https://github.com/pstember/codex.git`.
- Added initial TypeScript/Next.js scaffold files.
- Added seeded Atlas & Co. fixtures and harness contracts.
- Added initial SQLite persistence module backed by Node's built-in `node:sqlite`.
- Added initial tests for roles, Codex fixtures, campaign outputs, and storefront validation.
- Installed latest stable project dependencies via npm.
- Added Tailwind CSS v4, Biome, strict TypeScript, Vitest coverage, Playwright config, dependency freshness, and audit scripts.
- Added seeded demo users for Store Manager, Store Operator, and Guest.
- Added SQLite-backed sessions and demo auth service.
- Added protected Manager and Operator routes plus a public Guest storefront route.
- Added server actions for login, logout, and role-protected placeholder operations.
- Added initial in-app demo guidance, then removed the manual route once the product surface refocused on Store and Admin.
- Added Phase 1 tests for login, session expiry, role authorization, user/session persistence, and SQLite-backed auth lookup.
- Started the Metrics Copilot workflow with a server-side `answerMetricsQuestion` entry point.
- Added fixed commerce GraphQL schema validation for generated analytics queries.
- Rendered the Father’s Day golden question, Codex trace status, chart mapping, recommendations, and risk exclusions on the Manager page.
- Added SQLite persistence for Metrics Copilot traces.
- Added a Manager `Run analysis` action that saves an approved golden-query trace and refreshes the saved-runs panel.
- Expanded the Manager Metrics Copilot form to the five approved metric golden questions.
- Added saved-run detail rendering with generated GraphQL, validation status/errors, rationale, recommended products, and timestamp.
- Switched local Next dev/build scripts to webpack because Turbopack currently fails to load `node:sqlite`.
- Added Metrics Copilot chart/data-result mapping for product-table and funnel answers.
- Updated the Manager page to render chart-ready rows from the answer shape and added a saved-run comparison panel.
- Installed the Playwright Chromium browser for local UI smoke verification when relevant.
- Added Metrics Copilot Operator handoff output with campaign season, proposal prompt, selected product IDs, and risk exclusions.
- Reworked the Manager saved-run comparison into a visual status-and-inventory view backed by a tested domain comparison model.
- Added the first deterministic Operator campaign proposal flow from a saved Metrics Copilot trace.
- Added SQLite persistence for validated Operator campaign proposals.
- Added an Operator page handoff picker, campaign proposal generator action, and proposal review panel.
- Added a Playwright e2e smoke for the Manager saved-run to Operator proposal path.
- Added a tested Operator storefront config generation flow from valid campaign proposals.
- Added server-side storefront config validation for schema, campaign linkage, approved sections, and product references.
- Added SQLite persistence for generated storefront configs.
- Updated the Operator page so a valid proposal can be approved into a validated storefront config and reviewed by section.
- Extended the Playwright e2e smoke through proposal approval and storefront config rendering.
- Added a tested storefront publishing domain model with valid-config enforcement and rollback version creation.
- Added SQLite persistence for published storefront versions, active-version tracking, and inactive rollback targets.
- Added Operator publish controls and a Storefront Time Machine panel with active-version status and rollback actions for inactive versions.
- Updated the Guest storefront to render the active published version with baseline fallback and product placement details.
- Extended the Playwright e2e smoke through publishing and Guest storefront rendering.
- Expanded Storefront Time Machine with baseline/published version selectors and a tested comparison model for campaign, style, section, and product deltas.
- Extended the Playwright e2e smoke to assert the visible Time Machine comparison panel after publishing.
- Added an Operator revamp workflow that rewrites a valid Father’s Day proposal into a Secret Santa proposal.
- Added server-side validation that Secret Santa campaign proposals and storefront configs only include products priced at £50 or below.
- Added an Operator `Revamp for Secret Santa` action and extended the Playwright smoke through Father’s Day publish, Secret Santa revamp, seasonal storefront config generation, publish, and Guest rendering.
- Allowed `127.0.0.1` as a Next dev origin for local Playwright runs.
- Added validated storefront visual asset metadata with static hero assets for baseline, Father’s Day, and Secret Santa storefronts.
- Wired storefront generation to use the image harness for campaign hero assets and backfill legacy persisted storefront versions without visual metadata.
- Rendered hero visual previews and prompts in Operator review, static hero visuals on Guest, and a richer Time Machine strategic readout for hero copy and creative asset changes.
- Extended the Playwright smoke to assert the new visual prompt/readout during the Father’s Day to Secret Santa workflow.
- Added explicit Guest version selection on the public storefront using `?version=baseline` or a published version id, including inactive version previews without changing publication history.
- Extended the Playwright smoke so Guest selects the inactive Father’s Day version after Secret Santa is active.
- Removed Mission Control replay as the main demo surface so the app opens on the real public storefront.
- Added a real, configuration-gated Codex App Server harness path using `CODEX_HARNESS_MODE=app-server`, `codex app-server` stdio, ephemeral read-only threads, JSON-schema-constrained turns, and server-side validation before app persistence/rendering.
- Added a live custom Manager question path for App Server mode while preserving golden queries over static catalog data for fast rehearsal. Custom questions are translated by Codex into the fixed GraphQL schema, validated, executed against the seeded Atlas catalog, rendered from real query results, and saved as normal Metrics traces.
- Moved deterministic Codex campaign/storefront fixtures into test support, changed the default runtime harness to static catalog derivation, and relabeled runtime hero media as static assets under `public/static-assets/`.
- Added anonymous storefront cart domain operations with persona-aware promotion pricing for demo-only carts.
- Added the Codex observability run-event foundation with persisted `codex_runs`, append-only `codex_run_events`, terminal summaries, and a basic SSE route for persisted run events.
- Added staff password auth for Manager and Operator, removed the seeded Guest account, added `/admin`, routed unauthenticated back-office access there, and kept Guests on the public storefront.
- Added deterministic London commerce source data for customers, addresses, orders, stock positions, returns, email events, and promotions.
- Expanded commerce GraphQL to validate and execute `customers`, `orders`, and `promotions` queries in addition to products.
- Moved the public storefront to `/`, kept `/store` as a compatibility redirect, and added a demo persona cart UI with targeted promotion totals.
- Added a Manager Codex live window backed by persisted run events for prompt/schema/generation/validation/query/save stages.
- Redesigned the Manager workspace as a credible analytics command center with portfolio KPIs, all-SKU metric exploration, opportunity scoring, risk/hold signals, saved-run comparison, and a sticky Codex observability rail.
- Refreshed the shared staff `AppChrome` shell so Manager and Operator back-office pages have a denser operational navigation frame.
- Added `/test` as a temporary strict data-question workbench with a message box, send button, visible assistant response, live right-side traceability drawer, and an all-data browser below the chat for every seeded product, customer, address, order, order item, inventory, return, email, promotion, campaign, storefront, and storefront-section record while rebuilding Metrics Copilot components before migrating them back into `/manager`.
- Replaced the generic `/test` Codex chat with a data-only harness where Codex classifies non-empty prompts as query or unsupported, generated GraphQL is validated and executed server-side, raw results are compacted into factual evidence packs, and final answers may only use supplied evidence.
- Extended commerce GraphQL with validated `returns`, `emailEvents`, and `stockPositions` roots so open data questions can cover return reasons, campaign engagement, and inventory position signals.
- Updated the `/test` traceability window to stream the complete exchange in real time through `/api/test/data-question`, including request receipt, query prompt, query response, validation, execution, evidence packing, final-answer prompt, and final-answer response.
- Added a backend-owned `commerceMetrics` catalog with 25 deterministic commerce metric IDs, product ranking calculators, row caps, truncation notes, and compact evidence-pack generation for promotion-readiness questions.
- Expanded product GraphQL fields with seeded raw metric inputs (`views`, `addToCartRate`, `purchaseCount`, and `tags`) so Codex-generated queries can fetch the data needed for backend calculations.
- Upgraded `/test` into an analytics workbench with dashboard KPIs, a metric lens, ranked promotion candidates, risk exclusions, return reason distribution, conversion outlier strip, channel mix, all-data drilldown, and a live `metrics-calculated` trace event.

- Public storefront catalog rework: `/` now renders a smart gift-shop experience from the active validated `StorefrontConfig`, resolves a hero product from the config, exposes all 29 fixture products with search/category filters and pagination, and moves the local cart into the top menu with a live item count and compact cart panel. Removed stale `Shop the edit`, `Demo persona`, and `Anonymous cart` public copy while keeping Manager/Operator version preview controls gated.
- Basic storefront hero refresh: replaced the old placeholder baseline illustration with a dedicated basic-theme hero shot, aligned the baseline/default visual metadata to that asset, and made basic profile resets restore the matching hero image as well as the palette.
- Public storefront warning cleanup: marked the above-the-fold hero product image as eager-loading to address the LCP warning, and added `suppressHydrationWarning` on the root `<body>` so extension-injected attributes like `wotdisconnected` do not trigger React hydration noise in development.
- Public storefront asset hardening: restored `/static-assets/baseline-hero.svg` as a compatibility asset, normalized legacy baseline hero paths to `/static-assets/basic-hero.svg` on read, added stronger preload/eager hints for the above-the-fold hero product image, and made generated App Server hero images attempt automatic JPEG conversion through `sips` before falling back to the original saved file.
- Generated hero asset naming: updated future Codex App Server storefront hero outputs to use readable names like `halloween-storefront-hero.jpg` with numeric suffixes for repeats, and created readable JPEG aliases for the current local generated event hero PNGs while keeping the old hash-named PNGs available for existing local draft references.

## Test Status

- `npm run typecheck`: passing.
- `npm run lint`: passing.
- `npm test`: passing, 20 files and 115 tests.
- `npm run coverage`: passing at 93.04% statements, 92.92% lines, 96% functions, and 81.7% branches.
- `npm run build`: passing with `next build --webpack`.
- Playwright e2e smoke: passing after sandbox escalation for `/admin` Manager login, saved-run creation with Codex live-window events, Operator login, handoff selection, Father’s Day proposal/config/publish, Secret Santa revamp/config/publish, root storefront rendering, inactive version preview, and anonymous cart interaction. The sandboxed attempt failed because the Next test server could not bind to `0.0.0.0:3000`; Playwright now uses a throwaway SQLite file under `/private/tmp` for isolated browser runs.
- Live Codex App Server smoke: passed after sandbox escalation; a direct `codex app-server` stdio turn returned `{"ok":true,"source":"codex-app-server"}`. The first sandboxed attempt failed because Codex could not write its normal state under `~/.codex`.
- Live custom query App Server smoke: passed after sandbox escalation; Codex translated “Which under £50 products have the best margin and enough inventory?” into a valid `products(filter: { maxPrice: 50 })` GraphQL query with product fields.
- Focused anonymous cart promotions test: passing for add/update/remove operations and coffee-regulars Father’s Day promotion pricing.
- Focused Codex run events test: passing for persisted ordering and terminal summary.
- Scoped Biome check for Codex run files: passing.
- Focused auth/database tests: passing for staff password login, failed login rejection, no Guest account, password hashes, sessions, and role permission boundaries.
- Scoped Biome check for auth/admin files: passing.
- Focused rich commerce data/GraphQL tests: passing for relationship integrity, SQLite seed counts, and customer/order/promotion GraphQL execution.
- Integrated lint, typecheck, and unit test suite: passing after the refocus.
- `npm run build`: passing with `next build --webpack` after the route refocus.
- `npm run coverage`: passing at 91.39% statements, 91.48% lines, 96.49% functions, and 80% branches.
- Manager redesign checks: `npm run lint`, `npm run typecheck`, `npm test`, and `npm run build` passing. Playwright visual smoke passed against the built app on port 3002 for `/admin` Manager login and `/manager` desktop/mobile screenshots. The in-app browser blocked localhost navigation, so Playwright was used for verification instead.
- `/test` data-question harness focused checks: `npm test -- tests/dataQuestion.test.ts tests/commerceGraphql.test.ts tests/testDataCatalog.test.ts` passing, `npm run lint` passing, and `npm run typecheck` passing.
- Current full checks after the data-question harness: `npm run lint` passing, `npm run typecheck` passing, `npm test` passing with 17 files and 78 tests, `npm run coverage` passing at 92.59% statements, 92.62% lines, 97.23% functions, and 80.4% branches, and `npm run build` passing with `next build --webpack`.
- Product-ID label polish checks: `npm run lint` and `npm run typecheck` passing. Focused Playwright config smoke was blocked by an existing Next dev server on port 3000, so a direct Playwright Chromium check against the running server verified zero exact bare `Product IDs` fields and four contextual product-ID inputs.
- Feature-drop cap checks: RED tests first failed because storefront config allowed four products and App Server prompts/schema omitted the limit. After implementation, `npm test -- tests/storefront.test.ts tests/codexHarness.test.ts tests/storefrontGeneration.test.ts tests/storefrontAdaptation.test.ts`, `npm run lint`, and `npm run typecheck` are passing. A direct Playwright Chromium check against the running server verified `Feature drop 1` and `Feature drop 2` labels with no exact bare `Product IDs` field.
- `/test` Playwright smoke passed against the existing dev server at `http://127.0.0.1:3000/test` after sandbox escalation for Chromium; submitting an unsupported prompt populated the live trace with request receipt and the scoped data-only refusal in the response panel.
- Fixed the `/test` data-question query-plan JSON schema so Codex App Server accepts it for “Which item should I promote in my next campaign?”; focused regression now asserts every schema property is listed in `required`, with optional semantics represented by nullable fields. Focused `tests/dataQuestion.test.ts`, `npm run lint`, and `npm run typecheck` are passing.
- Metric catalog and `/test` dashboard checks: focused metric/data-question/GraphQL/catalog tests passing; `npm run lint`, `npm run typecheck`, `npm test`, `npm run coverage`, and `npm run build` passing. Static Playwright smoke passed against `http://127.0.0.1:3000/test` for KPI cockpit, metric lens, promotion-readiness table, Cast Iron Grill Press ranked candidate, return distribution, and channel mix.
- `/test` intent-classification routing check: `npm test -- tests/dataQuestion.test.ts`, `npm run lint`, `npm run typecheck`, `npm test`, and `npm run build` passing after moving unsupported/general-chat classification from the local keyword gate into the Codex query-planning step.
- `/test` prompt-scope examples check: added five example question buttons beside the chat input; `npm run lint`, `npm run typecheck`, focused data-question/metric tests, and `npm run build` passing.
- `/test` return date-range and metric-scope check: added `requestedFrom`/`requestedTo` return filtering, deterministic return-rate evidence for “What was my return rate from June 1 to June 3?”, and a metric coverage list beside the chat input. `npm run lint`, `npm run typecheck`, `npm test`, focused GraphQL/data-question tests, and `npm run build` passing.
- `/test` SMB mock-data realism check: added 19 Secret Santa/office-gift products for a 29-SKU catalog, expanded London customer segments to 48 customers, grew seeded order history to 260 orders with November/December seasonal volume, increased returns to 33 records, expanded email events to 144 rows, added an office Secret Santa promotion, and added `orderedFrom`/`orderedTo` GraphQL order filters for date-scoped order questions. `npm run lint`, `npm run typecheck`, `npm test`, `npm run coverage`, and `npm run build` passing.
- `/test` chat layout polish: moved Metric coverage below the chat card so it spans the central workbench column, then tightened the Message textarea stack to remove the stretched empty space above the input. `npm run lint` and `npm run typecheck` passed; Playwright desktop and mobile screenshots passed against the existing dev server at `http://127.0.0.1:3000/test` after sandbox escalation.
- `/test` observability polish: moved raw prompts/responses into per-step collapsed `Raw detail` blocks inside the live trace timeline so the rail stays scannable by default. `npm run lint` and `npm run typecheck` passed; Playwright interaction smoke confirmed raw detail blocks render closed by default.
- `/test` recent-run review: added the last five session runs under Observability, with click-to-revisit restoring the selected query, answer state, and trace timeline. `npm run lint` and `npm run typecheck` passed; intercepted Playwright smoke verified two-run ordering, revisit behavior, and collapsed raw details.
- Visual refresh: replaced the muted beige/forest styling with a prismatic d20-inspired color system across the shared app shell, public storefront, and `/test` workbench; added Poppins for headings and Inter for body/UI text through global CSS.
- Visual refresh checks: `npm run lint`, `npm run typecheck`, and `npm run build` passed. The in-app browser blocked localhost again, so Playwright screenshots were captured after sandbox escalation for `/test` desktop, `/test` mobile, and the public storefront against the existing dev server at `http://127.0.0.1:3000`.
- `/test` collapsed observability handle polish: added a clear `<` affordance to the closed rail and removed the nested hover box so the drawer reads as one clean openable handle. `npm run lint` and `npm run typecheck` passed; browser smoke verified the collapsed button exposes `<` plus the Observability label.
- Admin route refocus: removed the `/manager` and `/test` route surfaces, kept staff login on `/admin`, added two large workspace tiles for Insight and Manage storefront, moved the data-question workbench to `/insights`, and moved its streaming endpoint to `/api/insights/data-question`.
- Admin route refocus checks: `npm run lint`, `npm run typecheck`, `npm test`, `npm run coverage`, and `npm run build` passing. Focused Playwright route smoke passed against the existing dev server for `/admin` login, Insight and Manage storefront menu tiles, `/insights`, and the SKU explorer. Coverage is 93.81% statements, 93.84% lines, 97.75% functions, and 81.93% branches.
- Role-gated admin routes: added an Analyst staff role, moved Insight to `/admin/insights`, moved storefront management to `/admin/storefront`, removed `/insights` and `/operator` product routes, and greyed out disabled admin workspace buttons for roles without access.
- Public storefront admin controls: hid the version preview selector, active-version shortcut, and view-version submit from anonymous visitors and Analysts; Managers and Operators still see the control, and signed-in storefront headers now show name plus role.
- Role-gated admin route checks: `npm run lint`, `npm run typecheck`, `npm test`, `npm run coverage`, `npm run build`, and focused Playwright admin route smoke passing. Coverage is 93.81% statements, 93.84% lines, 97.75% functions, and 81.93% branches.
- Public storefront admin-control checks: RED Playwright test first failed because anonymous visitors could see `Preview version`; after the fix, `PLAYWRIGHT_REUSE_SERVER=1 npx playwright test tests/e2e/operator-handoff.spec.ts`, `npm run lint`, and `npm run typecheck` are passing. Playwright required sandbox escalation and reused the already-running local dev server on port 3000.
- Storefront adaptation checks: `npm run lint`, `npm run typecheck`, `npm test`, `npm run coverage`, and `npm run build` passing. Coverage is 92.46% statements, 92.41% lines, 96.21% functions, and 80.11% branches. A Playwright smoke against built Next on `127.0.0.1:3003` generated and published a Halloween draft, then verified the public storefront hero copy.
- Added a Storefront Visual Adaptation Studio on `/admin/storefront` with open event prompts, side-by-side live/draft preview, palette swatches, generated hero metadata, an observability rail, and a saved draft gallery that publishes through the existing storefront version model.
- Added a tested `storefrontAdaptation` domain flow, expanded storefront style tokens with optional palette data, added generated-event image harness metadata, and updated the public storefront to render event palettes for visitors.
- Removed the legacy Metrics handoff, Recent handoffs, and campaign proposal panels from `/admin/storefront`; added an interactive visual editor for the selected draft with color picker controls and image-only regeneration.
- Storefront visual editor follow-up checks: focused storefront/Codex harness tests, `npm run lint`, `npm run typecheck`, and `npm run build` passing after adding strict App Server storefront schema compatibility, color controls, and image-only regeneration.
- Navigation/logout cleanup checks: RED Playwright smoke first failed because the staff shell still showed Insight, Manage storefront, Manual, and Sign out, and the public storefront lacked Logout. After the fix, `npm run lint`, `npm run typecheck`, `npm test`, `npm run build`, and `PLAYWRIGHT_REUSE_SERVER=1 npx playwright test tests/e2e/operator-handoff.spec.ts` are passing.
- Added recent visual-run trace memory to the storefront observability rail so Operators can revisit previous adaptation exchanges in the same way the Insight workbench exposes recent runs.
- Fused Codex generation and save/edit controls into one storefront studio surface, and added properly wired draft deletion from the gallery without deleting already-published storefront versions.
- Tightened the fused storefront studio layout with compact color chips and an in-panel text readability preview that uses the selected palette.
- Expanded the storefront visual editor to frontend-only UI tokens, including muted text, borders, buttons, and button text, with the selected draft preview updating live before save.
- Reworked the lower storefront config review into an editable config workbench with saveable name/theme/density, hero prompt/alt text, section copy/product IDs, per-block Codex regeneration, and default/basic profile reset controls.
- Removed `/manual`, simplified the staff page header to Store, Admin, and Logout, and added Logout to the signed-in public storefront header.
- Moved the `/admin/storefront` observability rail to the same far-right open/closed layout used by Insight, with a dismiss button and slim callback handle.
- Added a top-level Config workbench master prompt that collects the selected draft's editable text, regenerates all copy in one validated pass, and saves the result back to the selected gallery draft while leaving the visual theme anchored to the draft selection.
- Added explicit `Save text` controls at the top of the Config workbench and a selected-draft save target note so Operators know where manual or generated text edits are persisted.
- Expanded `/admin/storefront` observability into a replay surface for all storefront Codex/GPT paths on the page. Visual adaptation streams, hero image regeneration, Config workbench master text generation, and per-section regeneration now write persisted Codex run events and appear in Recent runs for replay.
- Changed the logged-in storefront workspace so the selected generated draft is the current working preview until the Operator publishes it. The previous active storefront is no longer shown as a competing canvas preview.
- Reworked the comparison surface into an Active-to-current review with a side-by-side copy diff, showing active text and current draft text for changed selection name, hero title/copy, hero prompt, alt text, and matching section copy.
- Merged the old Config workbench and Storefront Time Machine cards into one `Edit and compare` workbench with `Edit current draft` and `Compare with active` zones.
- Storefront observability and Config workbench checks: RED tests first failed because the workbench regeneration boundary and recent Codex run listing did not exist. After implementation, `npm run lint`, `npm run typecheck`, `npm test`, `npm run coverage`, and `npm run build` are passing. Coverage is 92.63% statements, 92.58% lines, 96.32% functions, and 80.43% branches. Focused Playwright Operator smoke passed after sandbox escalation for the master prompt, `Generate all text`, selected-draft save note, `Save text`, and replayed `master text` recent-run controls.
- Current draft comparison checks: RED comparison test first failed because text diffs were not exposed. After implementation, `npm run lint`, `npm run typecheck`, `npm test`, `npm run coverage`, and `npm run build` are passing. Coverage is 92.62% statements, 92.56% lines, 96.36% functions, and 80.39% branches. Focused Playwright Operator smoke passed after sandbox escalation for `Current working draft`, `Active to current`, `Copy diff`, and Active/Current columns after generating all text.
- Merged edit/compare checks: focused Playwright Operator smoke passed after sandbox escalation for the single `Edit and compare` workbench and absence of the old `Storefront Time Machine` card. `npm run lint`, `npm run typecheck`, `npm test`, `npm run coverage`, and `npm run build` are passing. Coverage remains 92.62% statements, 92.56% lines, 96.36% functions, and 80.39% branches.
- App Server generation cleanup: runtime Codex generation now always uses the App Server harness instead of static fallback, storefront generation traces expose prepared prompts plus raw App Server responses, event adaptation/master text/section/image replay events include fuller prompt/response or image metadata, the Event field starts empty with `Event to customize for, e.g. World Cup`, and the duplicate Visual editor `Regenerate image` control was removed in favor of the hero-level `Regenerate hero image` action. Checks: focused storefront/Codex tests passing, `npm run lint`, `npm run typecheck`, `npm test`, `npm run coverage`, `npm run build`, and `npm run deps:outdated` passing. Coverage is 92.02% statements, 91.92% lines, 95.39% functions, and 80.22% branches. Focused Playwright Operator smoke passed serially after sandbox escalation while generating a visual draft and master text through App Server.
- Storefront hero image generation now uses Codex App Server image generation instead of placeholder metadata. The image harness requests one hero image, copies the App Server-saved PNG into ignored `public/generated-assets/`, validates `/generated-assets/` as a storefront visual path, and keeps generated prompt/alt metadata in the draft for publishing and replay. Checks: Codex App Server capability probe returned `imageGeneration: true`; a direct App Server image turn saved a PNG under `$CODEX_HOME/generated_images/`; focused image/storefront/Codex harness tests, `npm run lint`, `npm run typecheck`, `npm test`, `npm run coverage`, `npm run build`, and `npm run deps:outdated` passing. Coverage is 92.21% statements, 92.13% lines, 95.68% functions, and 80.86% branches.

- Public storefront catalog checks: added storefront catalog/helper tests for search, category filtering, pagination, category list, hero product resolution, and section product resolution. `npm run lint`, `npm run typecheck`, `npm test` passing with 21 files and 109 tests, `npm run coverage` passing at 92.45% statements, 92.36% lines, 95.84% functions, and 80.92% branches, and `npm run build` passing. Focused Playwright public-store smoke passed for anonymous controls, search, category filter, pagination, and cart badge; focused admin storefront smoke passed after sandbox escalation with a longer timeout for live App Server generation.
- Admin storefront simplification: removed the bottom Product placement/Campaign approval/Publish controls explainer cards, kept one selected-draft publish action, removed direct publishing from gallery cards, collapsed palette/version-history/internal config fields behind disclosures, and simplified the comparison surface to active/current status, style changes, product changes, and copy diff. Checks: RED-focused Playwright assertions first caught hidden-field selector issues, then `PLAYWRIGHT_REUSE_SERVER=1 npx playwright test tests/e2e/operator-handoff.spec.ts --grep "Staff admin opens"` passed after sandbox escalation; `npm run lint`, `npm run typecheck`, focused storefront tests, `npm test`, and `npm run build` are passing. The first parallel `npm run typecheck` collided with `next build` regenerating `.next/types`; rerunning typecheck by itself passed.
- Playwright database isolation hardening: disabled `reuseExistingServer` in `playwright.config.ts` after discovering that `PLAYWRIGHT_REUSE_SERVER=1` can reuse a normal dev server pointed at `.data/commerce.db` and create real storefront drafts during smoke tests. E2E now always starts its own server with `/private/tmp/commerce-copilot-e2e.db`.
- Product catalog imagery: generated 29 parallel Codex App Server product presentation images, promoted reusable catalog images into compressed JPGs under `public/static-assets/products/`, added validated `Product.image` metadata derived from SKU ids, and rendered those assets on reusable product cards while keeping spotlight hero imagery theme-specific. Checks: RED fixture test failed before image metadata existed, then `npm test -- tests/fixtures.test.ts tests/storefrontCatalog.test.ts`, `npm run typecheck`, `npm run lint`, `npm test`, `npm run coverage`, `npm run build`, and `npm run deps:outdated` passed. `npm run security:audit` still reports the known 2 moderate Next/PostCSS advisories where the offered force fix would downgrade Next to 9.3.3. In-app Browser blocked localhost with `ERR_BLOCKED_BY_CLIENT`, so Playwright Chromium was run after sandbox escalation against the existing dev server on `127.0.0.1:3000`; product images loaded without horizontal overflow and a screenshot was saved to `/private/tmp/storefront-product-images.jpg-check.png`.
- Storefront config workbench product-ID labels briefly included each section title so advanced editors could disambiguate repeated fields; this was superseded by numbered feature-drop labels.
- Storefront config workbench now uses fixed merchandising roles: `Hero product`, `Current offer`, and `Spotlight`; each configured section is capped at three product IDs in the storefront schema, App Server JSON schema, static generation paths, and Codex storefront-generation prompts. `All products` remains the fixed searchable catalog section.
- Storefront section contract update: added explicit section intents and exact order validation for Hero product, Current offer, and Spotlight; updated baseline/seasonal storefronts, static/fixture/App Server generation constraints, public storefront rendering, and `/admin/storefront` labels/previews to match. Checks: focused storefront/admin domain tests, `npm run lint`, `npm run typecheck`, `npm test`, `npm run coverage`, `npm run build`, and `npm run deps:outdated` passing. Coverage is 92.4% statements, 92.32% lines, 95.94% functions, and 80.52% branches. `npm run security:audit` still reports the known 2 moderate Next/PostCSS advisories where the available force fix would downgrade Next to 9.3.3. Focused Playwright admin/public smoke could not start because an existing Next dev server for this repo is already running on port 3000, and Next refused a second dev server even when Playwright was moved to port 3001.
- Shared admin observability shell: extracted the common open/collapse/reopen rail behavior into `AdminObservabilityRail` and wired both `/admin/insights` and `/admin/storefront` to pass page-specific trace content into it. The main workspace layouts no longer allocate grid columns for observability, keeping the rail as a removable demo overlay. Checks: `npm run lint`, `npm run typecheck`, `npm test`, `npm run coverage`, `npm run build`, and `npm run deps:outdated` passing. Coverage is 92.4% statements, 92.32% lines, 95.94% functions, and 80.52% branches. `npm run security:audit` still reports the known 2 moderate Next/PostCSS advisories where the available force fix would downgrade Next to 9.3.3. Focused Playwright with an isolated server was blocked by the existing Next dev server lock, so a direct Chromium smoke against the running `127.0.0.1:3000` dev server verified Insight and Storefront default-open, collapse, open-handle, and reopen behavior.
- Storefront edit/compare cleanup: confirmed the `Edit current draft` and `Compare with active` hero pills were static labels, not controls; removed both pills and tightened the selected-draft helper copy while keeping the always-rendered active/current comparison panel. Checks: RED Playwright assertion added for the absent faux controls, but an isolated Playwright run was blocked by the existing Next dev server lock; in-app browser smoke against the running `127.0.0.1:3000` server verified `Edit current draft` is absent and only the real `Compare with active` section remains. `npm run lint`, `npm run typecheck`, focused storefront workbench tests, `npm test`, `npm run coverage`, `npm run build`, and `npm run deps:outdated` passed. Coverage is 92.4% statements, 92.32% lines, 95.94% functions, and 80.52% branches. `npm run security:audit` still reports the known 2 moderate Next/PostCSS advisories where the available force fix would downgrade Next to 9.3.3.
- Storefront gallery preview/publish: renamed `Source version` to `Start from`, added an always-first non-deletable `Basic Atlas & Co.` gallery entry derived from `baselineStorefront`, added staff session-scoped `Preview for me`, and added `Publish for everyone` for both generated drafts and the basic storefront. Public `/` now honors the preview only for signed-in storefront staff and shows a clear-preview banner. Checks: RED storefront publishing tests failed before the gallery/preview helpers existed; after implementation, focused storefront publishing tests, `npm run lint`, `npm run typecheck`, `npm test`, `npm run coverage`, `npm run build`, and `npm run deps:outdated` passed. Coverage is 92.54% statements, 92.46% lines, 96% functions, and 80.49% branches. `npm run security:audit` still reports the known 2 moderate Next/PostCSS advisories where the available force fix would downgrade Next to 9.3.3. Isolated Playwright was blocked by the existing Next dev server lock, so a rebuilt temporary production server on `127.0.0.1:3005` plus direct Chromium smoke verified `Start from`, the basic card, preview/publish actions, the staff preview banner, and clear-preview behavior.
- Storefront admin visual redesign: rebalanced `/admin/storefront` around a wider dark Event storefront studio, a full-width Brand kit palette surface with visible primary and advanced tokens, a horizontal style gallery, and an asymmetric Edit and compare workbench with a sticky comparison column. Checks: `npm run lint`, `npm run typecheck`, and focused storefront tests passed. Isolated Playwright was blocked by the existing Next dev server lock on port 3000 even when moved to port 3001, so direct Chromium smoke against the running `127.0.0.1:3000` dev server verified desktop/mobile rendering, observability controls, and zero horizontal overflow. Screenshots were saved to `/private/tmp/admin-storefront-redesign-desktop.png` and `/private/tmp/admin-storefront-redesign-mobile.png`.
- Public hero crop contract refresh: redesigned `/` into a shorter integrated hero banner with an in-hero product image module, added validated `visualAsset.composition` metadata for the shared `storefrontHeroWide` `14 / 9` crop, aligned Codex hero-image prompts with the live crop/safe-copy zone, and updated admin previews/gallery cards to render the same aspect and focal rules. Checks: focused storefront/image/database tests, `npm run typecheck`, `npm run lint`, `npm test`, and `npm run build` passed.
- Basic-theme hero refresh checks: `npm test -- tests/storefront.test.ts tests/storefrontPublishing.test.ts tests/storefrontAdaptation.test.ts tests/imageHarness.test.ts` passing, and `npm run typecheck` passing.
- Public storefront warning cleanup checks: `npm run typecheck` passing. Browser automation verification was attempted, but local headless Chromium launch is blocked in this environment by a macOS Mach port permission error before page navigation begins.
- Public storefront asset hardening checks: focused image/storefront/database tests, `npm run typecheck`, `npm run lint`, and `npm run build` passing. Local tooling has `sips` for JPEG conversion but no working WebP encoder (`cwebp`/ImageMagick unavailable and `sips` cannot write WebP), so automatic conversion currently targets JPEG.
- Generated hero asset naming checks: `npm test -- tests/imageHarness.test.ts tests/storefront.test.ts`, `npm run typecheck`, `npm run lint`, and `npm run build` passing.
- App Server storefront schema regression fix: reproduced the invalid structured-output contract with a RED harness test for nested JSON-schema `required` arrays, added `visualAsset.composition.objectPosition` to the App Server storefront schema, and broadened Codex generation tests across query, insight, storefront adaptation, static branch derivation, data-question generation, image harness, and run events. Checks: focused Codex generation suite passed with 5 files and 32 tests before broadening; final `npm run lint`, `npm run typecheck`, `npm test`, `npm run coverage`, `npm run build`, and `npm run deps:outdated` passed. Coverage is 93.04% statements, 92.92% lines, 96% functions, and 81.7% branches. `npm run security:audit` still reports the known 2 moderate Next/PostCSS advisories where the available force fix would downgrade Next to 9.3.3.
- Storefront observability raw payload restore: Codex generation traces now preserve a raw App Server request envelope with the output schema and the raw response text, while the storefront adaptation stream shows the full incoming adaptation request, resolved source storefront, request payload, raw response, and parsed response in `Raw detail`. Checks: focused Codex/storefront observability tests, `npm run lint`, `npm run typecheck`, `npm test`, and `npm run build` passed.

## Dependency Status

- `npm run deps:outdated`: passing with no outdated dependencies reported.
- npm itself reports a newer minor global version, but global tooling was not changed.
- Latest check in this session after the admin route refocus: `npm run deps:outdated` passed with no outdated dependencies reported.
- Latest check in this session after role-gated admin routes: `npm run deps:outdated` passed with no outdated dependencies reported.
- Latest check in this session after storefront visual adaptation: `npm run deps:outdated` passed with no outdated dependencies reported.
- Latest check in this session after Config workbench replay: `npm run deps:outdated` passed with no outdated dependencies reported.
- Latest check in this session after current draft comparison: `npm run deps:outdated` passed with no outdated dependencies reported.
- Latest check in this session after merged edit/compare workbench: `npm run deps:outdated` passed with no outdated dependencies reported.
- Latest check in this session after App Server generation cleanup: `npm run deps:outdated` passed with no outdated dependencies reported.
- Latest check in this session after live storefront image generation: `npm run deps:outdated` passed with no outdated dependencies reported.
- Latest check in this session after the public storefront catalog rework: `npm run deps:outdated` passed with no outdated dependencies reported.
- Latest check in this session after storefront edit/compare cleanup: `npm run deps:outdated` passed with no outdated dependencies reported.
- Latest check in this session after storefront gallery preview/publish: `npm run deps:outdated` passed with no outdated dependencies reported.

## Security Status

- `npm run security:audit`: completed with 2 moderate advisories from Next's nested PostCSS dependency and exits non-zero because no safe current fix is available.
- The suggested force fix would downgrade Next to 9.3.3, so remediation is deferred and documented in `docs/security.md`.
- Latest check in this session after the admin route refocus: `npm run security:audit` still reports the same 2 moderate Next/PostCSS advisories; no safe compatible fix was applied.
- Latest check in this session after role-gated admin routes: `npm run security:audit` still reports the same 2 moderate Next/PostCSS advisories; no safe compatible fix was applied.
- Latest check in this session after storefront visual adaptation: `npm run security:audit` still reports the same 2 moderate Next/PostCSS advisories; no safe compatible fix was applied.
- Latest check in this session after Config workbench replay: `npm run security:audit` still reports the same 2 moderate Next/PostCSS advisories; no safe compatible fix was applied.
- Latest check in this session after current draft comparison: `npm run security:audit` still reports the same 2 moderate Next/PostCSS advisories; no safe compatible fix was applied.
- Latest check in this session after merged edit/compare workbench: `npm run security:audit` still reports the same 2 moderate Next/PostCSS advisories; no safe compatible fix was applied.
- Latest check in this session after App Server generation cleanup: `npm run security:audit` still reports the same 2 moderate Next/PostCSS advisories; no safe compatible fix was applied because the available force fix would downgrade Next to 9.3.3.
- Latest check in this session after live storefront image generation: `npm run security:audit` still reports the same 2 moderate Next/PostCSS advisories; no safe compatible fix was applied because the available force fix would downgrade Next to 9.3.3.
- Latest check in this session after the public storefront catalog rework: `npm run security:audit` still reports the same 2 moderate Next/PostCSS advisories; no safe compatible fix was applied because the available force fix would downgrade Next to 9.3.3.
- Latest check in this session after storefront edit/compare cleanup: `npm run security:audit` still reports the same 2 moderate Next/PostCSS advisories; no safe compatible fix was applied because the available force fix would downgrade Next to 9.3.3.
- Latest check in this session after storefront gallery preview/publish: `npm run security:audit` still reports the same 2 moderate Next/PostCSS advisories; no safe compatible fix was applied because the available force fix would downgrade Next to 9.3.3.

## Known Gaps

- Codex App Server is available locally through `codex app-server`, and runtime Codex generation now uses the `stdio://` App Server adapter as the required product harness with validated JSON output.
- UI is functional for public storefront/cart, staff auth/navigation, the `/admin` workspace menu, `/admin/insights` data-question workbench, Codex run observability, and Operator publishing.
- Login failure redirects to `/admin?error=invalid` and renders an inline staff auth error.
- Phase 1 auth is demo-grade and intentionally not a production auth provider.
- `/admin/insights` streams query planning, validation, evidence preparation, and final-answer events for the current run instead of persisting legacy metrics traces.
- `/` now renders the active published version with baseline fallback, validated hero visuals, explicit staff-only baseline/published-version selection, smart gift-shop catalog browsing, and a simple local cart without promo pricing; `/store` redirects to `/`.
- `/admin/storefront` is the single Operator merchandising workflow: event/theme adaptation drafts can be generated, reviewed, edited, published, and compared through the storefront version model.
- Operators can now ask Codex for open event visual adaptations, review generated copy/palette/live hero images, save drafts, publish them, and show visitor-facing palette/copy/image changes through the normal storefront version model.
- Mission Control/Loom replay, `/manager`, `/test`, `/insights`, and `/operator` have been removed from the product surface in favor of human-driven demo flow through `/admin`, `/admin/insights`, and `/admin/storefront`.

## Next Recommended Task

Human testing of the live public-store to `/admin`, then Insight or Manage storefront.
